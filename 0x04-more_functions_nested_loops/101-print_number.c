#include "main.h"
#include <stdio.h>
/**
 * print_number -prints int with putchar
 * @n: takes number, hello
 *
 * Return: void
 */
void print_number(int n)
{
insigned int n;

if (n < o)
{
_putchar('-')
m = n;
}
if (m / 10 != 0)
print_number(m / 10);
_putchar((m % 10) + '0');
}
